const gragh = () => {
  const weekLaundryBuddy = gon.weekLaundryBuddy
  const weekLaundryPartner = gon.weekLaundryPartner
  const weekDishesBuddy = gon.weekDishesBuddy
  const weekDishesPartner =gon.weekDishesPartner
  const weekCookingBuddy = gon.weekCookingBuddy 
  const weekCookingPartner = gon.weekCookingPartner
  const weekCleanRoomBuddy = gon.weekCleanRoomBuddy
  const weekCleanRoomPartner = gon.weekCleanRoomPartner
  const weekBathroomBuddy = gon.weekBathroomBuddy
  const weekBathroomPartner = gon.weekBathroomPartner
  const weekTrashBuddy = gon.weekTrashBuddy
  const weekTrashPartner = gon.weekTrashPartner
  const weekToiletBuddy = gon.weekToiletBuddy 
  const weekToiletPartner = gon.weekToiletPartner

  const buddyName = gon.buddyName
  const partnerName =  gon.partnerName

};

window.addEventListener("turbo:load",gragh);